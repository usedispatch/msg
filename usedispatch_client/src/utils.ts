import {
  Connection,
  PublicKey
} from '@solana/web3.js';
import {
  TOKEN_PROGRAM_ID
} from '@solana/spl-token';

export async function getNFTsForOwner(
  connection: Connection,
  publicKey: PublicKey
) {
}
